name: Run CRM Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - name: Audit dependencies
        run: |
          pip install pip-audit
          pip-audit -r requirements.txt --progress-spinner off
      - run: pytest tests/ -v
      - name: Run pipeline
        env:
          HUBSPOT_API_KEY: ${{ secrets.HUBSPOT_API_KEY }}
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GCP_CREDENTIALS_JSON }}
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "Live CRM sync skipped on pull_request."
            exit 0
          fi

          if [ -z "$HUBSPOT_API_KEY" ] || [ -z "$GCP_PROJECT_ID" ] || [ -z "$GOOGLE_APPLICATION_CREDENTIALS" ]; then
            echo "Live CRM sync skipped because one or more required secrets are not configured."
            exit 0
          fi

          python src/pipeline.py
